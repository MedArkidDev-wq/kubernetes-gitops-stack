apiVersion: v1
kind: Service
metadata:
  name: backend-api-service
  namespace: production
  labels:
    app: backend-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 5000
      protocol: TCP
  selector:
    app: backend-api
